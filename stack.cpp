#include <iostream>

int stack(vector[]){
}

int main( ) {
    int x, vector[x];
    std::cout << "Enter the vector size:" << std::endl;
    std::cin >> x >> std::endl;

    for(ii = 0; ii < x ; ii++) {
        vector[ii] = rand();
        std::cout << "Vector[%d]: ", ii << vector[ii] << std::endl;

        return 0;
    }
    for(ii = 0; ii < x; i++){
        if(vector[ii] > vector[ii+1]){
            
        }
    }
}